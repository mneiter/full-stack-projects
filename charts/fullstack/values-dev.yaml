global:
  environment: dev
frontend:
  image: nextjs-frontend:latest
  replicaCount: 1
backend:
  image: fastapi-backend:latest
  replicaCount: 1
mongo:
  image: mongo:6
  storage: 1Gi
ingress:
  host: dev.localhost

{{- if .Values.ingress.enabled }}
Your fullstack application has been successfully deployed! 🚀

Access it at:

  🌐 Frontend:       http://{{ .Values.ingress.host }}/
  📘 API Docs:       http://{{ .Values.ingress.host }}/api/docs

To get the list of all deployed resources:
  kubectl get all -l app.kubernetes.io/instance={{ .Release.Name }}

To uninstall the release:
  helm uninstall {{ .Release.Name }}
{{- else }}
Ingress is currently disabled (ingress.enabled = false).

You can port-forward manually to test your services, for example:
  kubectl port-forward svc/frontend 8080:3000
  kubectl port-forward svc/backend 8000:8000

Then visit:
  http://localhost:8080/
  http://localhost:8000/api/docs
{{- end }}

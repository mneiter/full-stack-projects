apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  selector:
    app: frontend
  ports:
    - port: {{ .Values.frontend.containerPort }}
      targetPort: {{ .Values.frontend.containerPort }}
